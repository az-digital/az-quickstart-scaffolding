name: Security workflow

on:
  push:
  schedule:
    - cron:  '0 13 * * *'

jobs:
  dependency-audit:
    name: Dependency Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Install dependencies
        run: |
          composer install
      - name: Check dependencies for vulnerabilities
        run: |
          composer security-check
  code-check:
    name: Static Code Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Install dependencies
        run: |
          composer global require hirak/prestissimo
          if [ $(git ls-remote --heads https://github.com/az-digital/az_quickstart.git $GITHUB_REF | wc -l) = 1 ]; then PROFILE_BRANCH="$GITHUB_REF"; else PROFILE_BRANCH=master; fi
          composer require --no-update az-digital/az_quickstart:dev-${PROFILE_BRANCH}
          composer install -o
      - name: Run static code analysis
        run: |
          composer phpcs
